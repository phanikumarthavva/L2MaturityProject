server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location /health {
        default_type text/plain;
        return 200 'ok';
    }
    location / {
        try_files $uri $uri/ /index.html;
    }
}
